product: android/app

dependencies:
  - ../shared
  - androidx.activity:activity-compose:1.7.2
#  - androidx.core:core-ktx

settings:
  compose:
    enabled: true
  junit: junit-4
  kotlin:
    serialization:
      enabled: true
#      version: 1.10.0
  android:
    parcelize:
      enabled: true
    resourcePackaging:
      excludes:
        - "DebugProbesKt.bin"

